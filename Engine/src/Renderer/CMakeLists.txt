set (renderer_src

    VertexBuffer.cpp
    VertexLayout.cpp
    ElementBuffer.cpp
    VertexArray.cpp

    Renderer.h
    VertexBuffer.h
    VertexArray.h
    VertexLayout.h
    ElementBuffer.h
    )

add_source("${renderer_src}")
